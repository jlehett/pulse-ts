import { VictoryEffectNode } from './VictoryEffectNode';

describe('VictoryEffectNode', () => {
    it('exports the node function', () => {
        expect(typeof VictoryEffectNode).toBe('function');
    });
});
