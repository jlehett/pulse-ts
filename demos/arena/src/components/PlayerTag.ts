import { Component } from '@pulse-ts/core';

/**
 * Marker component that identifies a node as a player.
 * Attach via `useComponent(PlayerTag)` in `LocalPlayerNode`.
 */
export class PlayerTag extends Component {}
